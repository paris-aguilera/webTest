---
import '@fontsource-variable/alexandria';
import { Image } from 'astro:assets';
import ailenImg from '../assets/ailenWeb.png';
import castizaImg from '../assets/castizaWeb.png';
import casinoImg from '../assets/casinoGoldWeb.png';
import kiuboImg from '../assets/kiuboWeb.png';

const proyectosData = [
  {
    titulo: "Kiubo",
    descripcion: "Aplicación mobile de gestión de tareas para la tercera edad.",
    imagen: kiuboImg,
    tags: ["Figma", "Illustrator", "Photoshop", "UX UI"],
    url: `${import.meta.env.BASE_URL}/kiubo`,
    classText: "proyecto-info entradaDesdeIzquierda-style",
    classImg: "proyecto-imagen-enlace entradaDesdeDerecha-style circulo-difuminado"
  },
  {
    titulo: "Ailenahuel.cl",
    descripcion: "Portafolio web creado en Astro y diseñada por mí con ❤.",
    imagen: ailenImg,
    tags: ["Figma", "Illustrator", "Astro", "UX UI"],
    url: "#",
    classText: "proyecto-info entradaDesdeDerecha-style",
    classImg: "proyecto-imagen-enlace entradaDesdeIzquierda-style circulo-difuminado"
  },
  {
    titulo: "My Casino Gold",
    descripcion: "Práctica profesional como diseñadora de la app, creadora de imágenes para publicidad y community manager.",
    imagen: casinoImg,
    tags: ["Figma", "Illustrator", "Photoshop"],
    url:`${import.meta.env.BASE_URL}/myCasinoGold`,
    classText: "proyecto-info entradaDesdeIzquierda-style",
    classImg: "proyecto-imagen-enlace entradaDesdeDerecha-style circulo-difuminado"
  },
  {
    titulo: "Castiza Beer",
    descripcion: "Proyecto de branding y diseño de interfaz para una cervecería artesanal.",
    imagen: castizaImg,
    tags: ["Figma", "IA", "Photoshop", "UX UI"],
     url:`${import.meta.env.BASE_URL}/castizaBeer`,
    classText: "proyecto-info entradaDesdeDerecha-style",
    classImg: "proyecto-imagen-enlace entradaDesdeIzquierda-style circulo-difuminado"
  }
];
---

<section class="proyectos-container" id="proyectos">
    <h2 class="titulo-seccion">Proyectos 💻</h2>
    
    <div class="proyectos-lista">
        {proyectosData.map(proyecto => (
            <article class="proyecto-item">
                
                <div class={proyecto.classText}>
                    <h3>{proyecto.titulo}</h3>
                    <p class="proyecto-descripcion section-description">{proyecto.descripcion}</p>
                    <div class="tags-container ">
                        {proyecto.tags.map(tag => <span class="card-tag">{tag}</span>)}
                    </div>
                    <a href={proyecto.url} class="ver-proyecto-button">
                        <span>Ver proyecto</span>
                        <i class="fas fa-arrow-right"></i>
                    </a>
                </div>

                <a href={proyecto.url} class={proyecto.classImg}>
                    <Image src={proyecto.imagen} alt={`Vista previa del proyecto ${proyecto.titulo}`} class="proyecto-imagen"/>
                </a>

            </article>
        ))}
    </div>
</section>

<style>

.proyectos-container {
    font-family: 'Alexandria Variable', sans-serif;
    padding: 30px 150px 200px;
    margin: 0 auto;
    box-sizing: border-box;
    max-width: 1920px;
}

.titulo-seccion {
    color: white;
    font-size: clamp(30px, 4vw, 50px);
    margin-bottom: 20px;
}



.proyectos-lista {
    display: flex;
    flex-direction: column;
    gap: 180px;
    padding-top: 50px;
    
}

.proyecto-item {
    display: flex;
    gap: 50px;
    align-items: center;
    padding: 20px 0px 30px 0px;
    
}

.proyecto-item:nth-child(even) {
    flex-direction: row-reverse;
}

.proyecto-info {
    flex: 1;
    color: white;
    max-width: 25%;
}

.proyecto-imagen-enlace {
    flex: 1.2; 
    display: block;
}

.proyecto-imagen {
    width: 100%;
    height: auto;
    display: block;
    transition: transform 0.3s ease; 
}

.proyecto-imagen-enlace:hover .proyecto-imagen {
    transform: scale(1.05);
}

.proyecto-titulo {
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin-bottom: 20px;
}

.proyecto-descripcion {
    font-size: clamp(16px, 1.5vw, 20px);
    line-height: 1.6;
    margin-bottom: 30px;
}

.tags-container {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-bottom: 30px;
}

.card-tag {
    display: inline-block;
    font-size: 0.8rem;
    font-weight: bold;
    padding: 5px 12px;
    border-radius: 5px;
    background-color: rgba(67, 39, 191, 0.15);
    color: #8A78F5;
}

.ver-proyecto-button {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background-color: #4327BF;
    border: 1px solid #4327BF;
    color: white;
    padding: 10px 20px;
    font-weight: 600;
    font-size: 0.9rem;
    text-decoration: none;
    transition: all 0.3s ease;
}
.ver-proyecto-button:hover {
    transform: translateY(-1px);
    background-color: #3a22a3;
}
.ver-proyecto-button:hover {
    background-color: #5a40d8;
}
.ver-proyecto-button .fas {
    transition: transform 0.3s ease-in-out;
}
.ver-proyecto-button:hover .fas {
    transform: translateX(5px);
}

@media (max-width: 1024px) {
    .proyectos-container {
        padding: 40px 20px 200px;
    }
    
    .proyectos-lista {
        gap: 150px;
    }
    
    .proyecto-item,
    .proyecto-item:nth-child(even) {
        flex-direction: column;
        gap: 30px;
    }

    .proyecto-imagen-enlace {
        order: -1;
        width: 100%;
    }

    .tags-container, .ver-proyecto-button {
        margin-left: auto;
        margin-right: auto;
    }
    .proyecto-info {
    max-width: 100%;
}
}

@media (max-width: 768px) {
    .proyectos-container {
        padding: 40px 20px  200px;
    }
    .titulo-seccion {
        margin-bottom: 60px;
    }
}

.circulo-difuminado {
  position: relative; 
  z-index: 1;
}

.circulo-difuminado::before {
  content: ''; 
  position: absolute; 
  width: 60%;
  height: 60%;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #8E43F7CC;
  border-radius: 50%; 
  filter: blur(120px);
  z-index: -1; 
}

 .circulo-difuminadoCasino {
    position: relative;
    z-index: 1;
  }

  .circulo-difuminadoCasino::before {
    content: '';
    position: absolute;
    width: 60%;
    height: 60%;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background-color: #FFF1C9B2 ;
    border-radius: 50%;
    filter: blur(120px);
    z-index: -1;
  }
</style>

<script>
document.addEventListener('DOMContentLoaded', () => {
    const observerOptions = {
        root: null, 
        rootMargin: '0px 0px -100px 0px', 
        threshold: 0.05 
    };
    const sectionsToAnimate = document.querySelectorAll('.entradaDesdeIzquierda-style, .entradaDesdeDerecha-style');
    const observer = new IntersectionObserver((entries, observer) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('is-visible');
                entry.target.classList.remove('is-hidden'); 
            } else {
                entry.target.classList.remove('is-visible');
                entry.target.classList.add('is-hidden'); 
            }
        });
    }, observerOptions);
    sectionsToAnimate.forEach(section => {
        observer.observe(section);
    });
});
</script>