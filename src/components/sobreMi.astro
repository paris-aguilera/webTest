---
import '@fontsource-variable/alexandria';
import photoshop from '../assets/Photoshop.png';
import illustrator from '../assets/Illustrator.png';
import indesing from '../assets/InDesign.png';
import figma from '../assets/Figma.png';
import blender from '../assets/Blender.svg';
import vid from '../assets/video.mp4';
---
<script src="../styles/efectos.js" type="module"></script>
<div class="sobreMi-container">
    <div class="sobreMi-card">
        
        <section class="info-section  entradaDesdeIzquierda-style">
            <h2 class="titulo-card">Sobre mí 👋</h2>
            <p>Soy Ailen Nahuel, una diseñadora UX/UI apasionada por combinar estética y funcionalidad.</p>
            <p>Actualmente me enfoco en crecer en el diseño digital, interfaces y experiencia de usuario.</p>

             <div class="contacto-info">
                <div class="logos-grid">
                    <div class="logo-wrapper">
                        <img src={photoshop.src} alt="Logo Photoshop" class="tool-logo"/>
                    </div>
                    <div class="logo-wrapper">
                        <img src={illustrator.src} alt="Logo Illustrator" class="tool-logo"/>
                    </div>
                    <div class="logo-wrapper">
                        <img src={indesing.src} alt="Logo InDesign" class="tool-logo"/>
                    </div>
                    <div class="logo-wrapper">
                        <img src={figma.src} alt="Logo Figma" class="tool-logo"/>
                    </div>
                    <div class="logo-wrapper">
                        <img src={blender.src} alt="Logo Blender" class="tool-logo"/>
                    </div>
                </div>
            </div>
        </section>

        <section class="avatar-section entradaDesdeDerecha-style">
            <div class="avatar-wrapper">
                <iframe
                    src="https://player.mux.com/Q00001rtIJspHpJDLGF97yZw7v02IqdYGQz9ME02TskzNws?metadata-video-title=ailen&video-title=ailen&primary-color=%23f3e2e2&autoplay=true&loop=true&muted=true"
                    style="width: 100%; border: none; aspect-ratio: 5/7; border-radius: 10px;"
                    allow="accelerometer; gyroscope; autoplay; encrypted-media; picture-in-picture;"
                    allowfullscreen
                ></iframe>
            </div>
        </section>
        
    </div>
</div>

<style>
    /* -------------------------------------- */
    /* ESTILOS DE FUENTE Y CONTENEDOR PRINCIPAL */
    /* -------------------------------------- */
    .sobreMi-container {
        font-family: 'Alexandria Variable', sans-serif;
        padding: 50px 0;
        max-width: 100%;
        box-sizing: border-box;
        display: flex; /* Para centrar la tarjeta en el view-port */
        justify-content: center;
    }

    /* -------------------------------------- */
    /* TARJETA (CARD) Y EFECTOS VISUALES */
    /* -------------------------------------- */
    .sobreMi-card {
        /* Dimensiones y Bordes */
        width: 100%;
        border-radius: 40px;
        padding: 70px;
        box-sizing: border-box;
        color: white;
        
        /* Layout de Columnas */
        display: flex;
        gap: 40px;
        
        /* 🚀 COLOR BASE ACTUALIZADO Y EFECTO VIVIENTE */
        
        /* Color base: #17151F con un poco de transparencia para el efecto 'glass' */
        background-color: rgba(23, 21, 31, 0.9); /* #17151F = rgb(23, 21, 31) */
        
        /* Degradado de Lenta Animación para el efecto "viviente" */
        background-image: linear-gradient(
            to right, 
            rgba(39, 37, 51, 0.9),  /* Tono ligeramente más claro de #17151F */
            rgba(23, 21, 31, 0.9),  /* Color base */
            rgba(39, 37, 51, 0.9)   /* Tono ligeramente más claro */
        );
        /* 300% de tamaño para asegurar que el movimiento sea visible */
        background-size: 300% 100%; 
        /* Animación continua y sin pausa (linear infinite) */
        animation: slow-shift 15s linear infinite; /* Aumenté el tiempo a 15s para ser más lento */
        
        /* Borde suave para el efecto "frosted glass" */
        backdrop-filter: blur(5px); 
        -webkit-backdrop-filter: blur(5px);
    }
    
    @keyframes slow-shift {
        /* 🚀 Cambiamos a solo dos pasos para un movimiento continuo sin invertir (alternate) */
        0% {
            background-position: 0% 50%;
        }
        100% {
            background-position: 100% 50%;
        }
    }

    /* -------------------------------------- */
    /* SECCIONES INTERNAS (Layout de Columnas) */
    /* -------------------------------------- */
     .info-section {
        padding-top: 30px; 
        flex: 3; 
        /* 🚀 Aumentamos el min-width de info-section para que tome más espacio antes de romperse */
        min-width: 55%; 
    }
    .avatar-section {
        flex: 2; 
        display: flex;
        justify-content: flex-end;
        align-items: flex-start;
        /* 🚀 Reducimos el min-width para darle más espacio al layout antes de apilarse */
        min-width: 200px; 
    }

    /* -------------------------------------- */
    /* TIPOGRAFÍA Y AVATAR */
    /* -------------------------------------- */
    .titulo-card {
        font-size: clamp(30px, 4vw, 50px);
        margin-bottom: 20px;
        font-weight: bold;
    }

    .info-section p {
        font-size: clamp(16px, 1.5vw, 20px);
        line-height: 1.6;
        margin-bottom: 15px;
    }
    
    .contacto-info {
        margin-top: 30px;
        padding-top: 20px;
        /* Ajuste la transparencia del borde para que coincida con el nuevo fondo oscuro */
        border-top: 1px solid rgba(255, 255, 255, 0.1); 
    }

    .avatar-wrapper {
        
            overflow: hidden;
            width: 100%; 
            max-width: 300px; 
            height: auto;
        }

         .avatar-wrapper iframe {
        border-radius: 40px;
    }

      @media (max-width: 850px) {
        .profile-avatar {
            --size: 100px; 
            transform: translate(-50%, calc(-1 * var(--size) / 2));
        }
        
        .sobreMi-card {
            margin-top: calc(100px / 2); 
            flex-direction: column; /* APILAR las columnas verticalmente */
            padding: 25px;
        }

        .info-section {
            padding-top: 20px; 
            min-width: 100%; /* El info-section debe ocupar todo el ancho al apilarse */
        }

        .avatar-section {
            order: -1; 
            justify-content: center; 
            margin-bottom: 30px;
            min-width: 100%;
        }
        
        .avatar-wrapper {
            max-width: 250px;
        }
    }

     .avatar-video {
        display: block;
        width: 100%;
        /* CLAVE: Asegura que el video llene el contenedor sin distorsión */
        height: 100%;
        object-fit: cover; 
        /* En caso de que el video tenga forma rectangular */
        border-radius: 10px;
    }

    
    .avatar-image {
        display: block;
        width: 100%;
        height: auto;
        object-fit: cover;
        border-radius: 10px;
    }
    
    /* -------------------------------------- */
    /* MEDIA QUERY PARA MOBILE */
    /* -------------------------------------- */
    @media (max-width: 768px) {
        .sobreMi-card {
            flex-direction: column; /* Apila las secciones verticalmente */
            padding: 25px;
        }
        
        .avatar-section {
            order: -1; 
            justify-content: center; /* Centra el avatar horizontalmente */
            margin-bottom: 30px;
            min-width: 100%;
        }
        
        .avatar-wrapper {
            max-width: 250px; /* Ajusta el tamaño del avatar en pantallas pequeñas */
        }
    }

    /* ... (Estilos anteriores de .sobreMi-container y .sobreMi-card) ... */

    /* -------------------------------------- */
    /* ESTILOS DE LOGOS DE HERRAMIENTAS */
    /* -------------------------------------- */
    
    .contacto-info {
        margin-top: 30px;
        padding-top: 20px;
        border-top: 1px solid rgba(255, 255, 255, 0.1); 
    }

    .logos-grid {
        display: flex;
        gap: 15px; 
        flex-wrap: wrap; 
        margin-top: 15px;
    }

    .logo-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
        
        width: 60px; /* Tamaño fijo, no cambiará con el hover */
        height: 60px; /* Tamaño fijo, no cambiará con el hover */
        border-radius: 10px; 
        
        /* 🚀 COLOR BASE ACTUALIZADO: Muy similar al #17151F */
        /* #17151F (Tarjeta) = rgb(23, 21, 31) */
        /* Nuevo Color (Wrapper) = rgb(30, 28, 38) -> Un poco más claro */
        background-color: rgb(30, 28, 38); 
        
        /* Borde difuminado y transición */
        box-shadow: 0 0 8px 2px rgba(0, 0, 0, 0.3); 
        transition: all 0.2s ease-in-out; 
        cursor: pointer;
    }

    /* 🚀 Efecto HOVER: Usamos transform: scale() para que NO MUEVA a los vecinos */
    .logo-wrapper:hover {
        /* Aumenta visualmente el tamaño un 5% (para un crecimiento de 3px en un 60px) */
        /* Si quieres un aumento exacto de 1px (de 60px a 61px), usa scale(1.0166) */
        transform: scale(1.05); 
        
        /* Intensifica el brillo al hacer hover */
        box-shadow: 0 0 12px 4px #4327BF; 
    }

    .tool-logo {
        width: 70%; 
        height: 70%;
        object-fit: contain;
        transition: all 0.2s ease-in-out;
    }

    /* -------------------------------------- */
    /* MEDIA QUERY PARA MOBILE */
    /* -------------------------------------- */
    @media (max-width: 768px) {
        /* ... (Estilos de mobile se mantienen) ... */
        
        .logos-grid {
            justify-content: center; 
        }
    }
</style>

<script is:inline>
// 🚀 MEJORA: Es buena práctica envolver el código del script en un 'DOMContentLoaded' 
// aunque en Astro con 'is:inline' al final del body no siempre es estrictamente necesario, 
// asegura que todos los divs existan antes de que el script los busque.

    document.addEventListener('DOMContentLoaded', () => {
        const observerOptions = {
            root: null, 
            rootMargin: '0px 0px -100px 0px', 
            threshold: 0.05 
        };

        const sectionsToAnimate = document.querySelectorAll('.entradaDesdeIzquierda-style, .entradaDesdeDerecha-style');

        const observer = new IntersectionObserver((entries, observer) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('is-visible');
                    entry.target.classList.remove('is-hidden'); 
                } else {
                    entry.target.classList.remove('is-visible');
                    entry.target.classList.add('is-hidden'); 
                }
            });
        }, observerOptions);

        sectionsToAnimate.forEach(section => {
            observer.observe(section);
        });
    });
</script>